fastapi
uvicorn
sqlalchemy
jinja2
python-multipart
python-dotenv
pydantic-settings
alembic
python-jose[cryptography]
passlib[bcrypt]

# Postgres driver (production / Vercel). Dev cục bộ vẫn dùng SQLite, không cần.
psycopg2-binary

# Dev / test
pytest
httpx
